# Dług Techniczny

> Ten plik jest generowany automatycznie (PATH-11).
> Agent aktualizuje go w trakcie implementacji — kolumna „Gdzie (plik:linia)"
> wypełnia się, gdy powstaną konkretne pliki.

Źródło: tabela funkcjonalności w `docs/CONTEXT.md`, zaakceptowana 2026-07-30.
Każda pozycja to świadome uproszczenie v1, nie błąd.

## Uproszczenia z tabeli funkcjonalności

| ID | Funkcjonalność | Co uproszczono | Co pomija | Koszt odkręcenia | Ryzyko | Plan naprawy | Gdzie (plik:linia) |
|----|----------------|----------------|-----------|------------------|--------|--------------|--------------------|
| TD-001 | F01 — Utworzenie profilu gry | Regiony HUD rysowane na jednej klatce referencyjnej; profil bez wersjonowania | Zmienne układy HUD: inna rozdzielczość, inny tryb gry, inny aspect ratio | ~1 dzień, 2–3 pliki | Nagranie w innej rozdzielczości niż klatka referencyjna daje przesunięte regiony i błędny OCR | Regiony zapisywać we współrzędnych względnych (0–1) zamiast pikselowych; dodać pole `sourceResolution` do profilu; przy imporcie skalować regiony | `backend/app/engines/definition/engine.py:58`; `backend/app/access/store/models.py:57` |
| TD-002 | F03 — Import nagrania | Ścieżka do pliku na lokalnym dysku, bez uploadu i kopiowania do przestrzeni projektu | Pracę na plikach zdalnych, kolejkę wielu nagrań naraz | ~0.5 dnia, 1 plik za abstrakcją źródła | Przeniesienie lub usunięcie pliku źródłowego psuje projekt bez ostrzeżenia | Wprowadzić interfejs `VideoSource` z implementacją lokalną od dnia 1; dodać walidację istnienia pliku przy otwarciu sesji | `backend/app/managers/workflow/material_use_cases.py:62`; `backend/app/access/store/models.py:116` |
| TD-003 | F04 — Próbkowanie klatek | Stały interwał (`ffmpeg`), domyślnie 1 klatka na sekundę | Detekcję zmian regionu HUD, odsiew niemal identycznych klatek | ~1 dzień, 1 plik | Dataset zapełnia się duplikatami; model przeucza się na powtarzalnych stanach HUD | Po próbkowaniu liczyć hash percepcyjny wycinków regionów i pomijać klatki poniżej progu różnicy | `backend/app/access/media/processing.py`; `backend/app/managers/workflow/worker.py`; `backend/app/access/store/repositories/frames.py` |
| TD-004 | F06 — OCR regionów | Jeden lokalny silnik OCR, bez fine-tuningu na czcionce HUD danej gry | Trening na foncie gry, wielojęzyczność, warianty stylizowane | ~2–3 dni, 1 moduł za interfejsem `OcrEngine` | Niska trafność na stylizowanych fontach; częściowo kompensuje ekran weryfikacji, ale rośnie praca ręczna | Utrzymać granicę `OcrEngine` od początku; po zebraniu pierwszego datasetu dotrenować model na własnych etykietach | `backend/app/access/ocr/protocol.py:11`; `backend/app/access/ocr/tesseract.py:225`; `backend/app/engines/definition/ocr_mapping.py:47` |
| TD-005 | F08 — Ekran weryfikacji | ZAMKNIĘTY 2026-08-04: F09 wciągnięty do v1. Zostaje wyłącznie prostokątny boks — brak wieloboków, masek i propagacji boksu między klatkami | Maski i tracking (F10) | — | Ręczna praca rośnie liniowo z liczbą klatek, gdy OCR jest słaby | Trigger dla F10 opisany w `SCALE.md`; do tego czasu mierzyć udział anotacji `manual` w manifeście eksportu | `backend/app/engines/review/engine.py:53`; `backend/app/managers/workflow/review_use_cases.py:104`; `backend/app/access/store/repositories/annotations.py:110`; `backend/app/access/store/repositories/checkpoints.py:267`; `backend/app/access/store/repositories/frames.py:227`; `backend/app/api/annotations.py:20`; `backend/app/api/frames.py:24`; `backend/app/managers/workflow/export_use_cases.py:134` |
| TD-006 | F11 — Eksport `COCO JSON` | Jeden plik `COCO` z zaakceptowanych klatek, bez podziału train/val | Splity, warianty formatu (YOLO), eksport przyrostowy | ~0.5 dnia, 1 plik | Podział datasetu trzeba wykonać poza narzędziem, ręcznie i powtarzalnie | Dodać parametr splitu i deterministyczny seed przy eksporcie | `backend/app/engines/coco/engine.py:42`; `backend/app/managers/workflow/export_use_cases.py:49`; `backend/app/access/store/repositories/exports.py:136` |
| TD-007 | F12 — Dashboard | Stan bieżącej sesji i stan GPU; brak historii i metryk | Wykresy, statystyki jakości, historię runów | ~1 dzień, 1–2 pliki | Brak wglądu w trend jakości między sesjami — regresja OCR pozostaje niewidoczna | Zapisywać podsumowanie każdego runu do bazy od dnia 1, nawet bez UI; widok dołożyć później | `frontend/src/features/dashboard/DashboardScreen.tsx:85` |
| TD-008 | F14 — Stan pipeline'u | Progres etapów odpytywany cyklicznie (polling), bez strumienia zdarzeń | Push, log per klatka | ~1 dzień, 2 pliki | Opóźniony obraz stanu przy długich nagraniach; użytkownik nie wie, czy etap stoi, czy pracuje | Wymienić polling na SSE po stronie serwera lokalnego, gdy pojawi się potrzeba | `backend/app/api/runs.py`; `backend/app/managers/workflow/manager.py` |
| TD-009 | F15 — Trwałość projektu | Lokalny `SQLite`, jeden projekt naraz | Wiele projektów, migracje schematu, kopie zapasowe | ~1 dzień, 1–2 pliki | Utrata pliku bazy = utrata stanu sesji; brak migracji utrudnia zmianę schematu po pierwszych danych | Trzymać schemat w jednym miejscu i dopisywać migracje od pierwszej zmiany; dodać eksport stanu projektu | `backend/app/access/store/models.py`; `backend/alembic/versions/0001_initial_schema.py`; `backend/alembic/versions/0002_reference_assets.py`; `backend/alembic/versions/0003_profile_asset_integrity.py`; `backend/alembic/versions/0004_durable_workflow.py`; `backend/app/access/store/repositories/{runs,frames,checkpoints}.py`; `backend/app/managers/workflow/recovery.py` |
| TD-010 | F16 — Powłoka interfejsu | Tokeny i klasy `.df-*` z baseline'u jako zwykły CSS, bez portu na Tailwind | Utility-first, warianty komponentów, tryb jasny | ~1–2 dni, warstwa stylów | Rozjazd między baseline'em Impeccable a późniejszymi iteracjami designu z `immersive-ui-factory` | Trzymać tokeny w jednym pliku wygenerowanym z `designs/baseline-impeccable/tokens.json`, żeby podmiana systemu była zmianą jednego źródła | `frontend/src/styles/tokens.css:7` |
| TD-011 | NFR-03 — Backup | Brak automatycznego backupu katalogu projektu i SQLite | Kopie rotacyjne, test odtworzenia, eksport stanu sesji | ~1 dzień, storage + skrypt/endpoint | Utrata katalogu projektu oznacza utratę całej sesji | Dodać atomowy snapshot bazy i manifestu; trigger: pierwszy dataset, którego odtworzenie kosztuje >1 dzień | `backend/app/config.py:82` |
| TD-012 | NFR-10 — CI | Gate jakości uruchamiany lokalnie, bez hostowanego CI | Automatyczne sprawdzanie każdego commita/PR | ~0.5 dnia, workflow CI | Można zmergować zmianę bez uruchomienia testów | Dodać CI dla backendu i frontendu; trigger: pierwszy współpracownik lub publikacja repo | `pyproject.toml:26`; `frontend/package.json:9` |
| TD-013 | FE-08 — Dostępność | Minimum a11y bez deklaracji pełnego WCAG AA | Pełny audyt czytnika ekranu, aria i automatyczne testy axe | ~1–2 dni, UI/testy | Część interakcji canvas może być niedostępna dla technologii asystujących | Dodać audyt WCAG/axe i alternatywną listę anotacji sterowaną klawiaturą; trigger: użytkownik zewnętrzny | `frontend/src/components/common/Button/Button.tsx:16`; `frontend/src/components/common/UiStates/UiStates.tsx:10`; `frontend/src/components/common/RegionOverlay/RegionOverlay.tsx:105`; `frontend/src/features/annotations/AnnotationList.tsx:26`; `frontend/src/features/exports/ExportsScreen.tsx:147` |
| TD-014 | F06 — Docelowy `OcrEngine` | Tesseract działa wyłącznie jako adapter `experimental`; wersjonowany Gate 2 jakości to FAIL (93,22%, 7/11 exact, minimum IoU 0); regresja utrwala też FAIL dla wcześniejszych 94,34% | Wybór i kalibrację docelowego silnika natywnych boksów per znak na prawdziwych HUD-ach | ~3–7 dni na reprezentatywny benchmark i nowy adapter; więcej przy treningu char-detectora | Błędne klasy i brakujące znaki; od 2026-08-04 v1 pozwala dorysować boks ręcznie (F09), więc kosztem słabego OCR jest praca człowieka, nie utracone klatki | Zebrać realne cropy kilku gier/fontów; porównać tuned Tesseract z char-level detector; wymagane bbox precision/recall, percentyle/minimum IoU i exact char+bbox. Trigger: przed uznaniem autolabelingu za produkcyjny lub przed packaged v1 | `backend/app/access/ocr/protocol.py`; `backend/app/engines/definition/ocr_evaluation.py`; `backend/tests/fixtures/expected-ocr/tesseract-5.5.3-evaluation-v2.json`; `docs/OCR_SPIKE.md` |
| TD-015 | Supply chain Tesseract | Zewnętrzna dystrybucja Windows UB Mannheim na `D:` ma pinowane runtime/model SHA-256, ale Authenticode nie weryfikuje się poprawnie | Weryfikowalny artefakt runtime do dystrybucji | ~1–2 dni dla własnego builda/pinningu albo wyboru poprawnie podpisanej dystrybucji | Podmiana binarki/modelu lub nieweryfikowalna dystrybucja | Dopuszczone tylko dev/spike. Przed packaged v1 użyć własnego builda z przypiętego source lub artefaktu z niezależnym podpisem/checksumem; adapter odrzuca `unknown`/mismatch przed OCR | `backend/app/access/ocr/tesseract.py`; `backend/app/config.py`; `docs/OCR_SPIKE.md`; `D:\tools\tesseract-5.5.3` (lokalne, poza repo) |
| TD-016 | Manifest fixture OCR | `manifest_sha256` jest polem opisowym, nie kontrolnym: generator i test liczą go z tego samego payloadu, a test i tak porównuje cały `fixture_manifest` | Niezależny łańcuch zaufania dla manifestu — hash nie wykrywa nic, czego nie wykryje porównanie payloadu | ~0,5 dnia: albo udokumentować pole jako identyfikator wersji, albo pinować manifest osobno od danych, z których jest liczony | Fałszywe poczucie kontroli integralności; przyszły agent może polegać na hashu zamiast na porównaniu payloadu | Zdecydować jedną z dwóch dróg: (a) opisać `manifest_sha256` wprost jako identyfikator wersji manifestu, (b) pinować oczekiwany manifest w teście jako stałą, wtedy hash zaczyna działać jako kontrola. Trigger: przy dodaniu kolejnego wersjonowanego raportu ewaluacji | `backend/tests/fixtures/generate_tk003_ocr_observation.py`; `backend/tests/test_ocr_evaluation.py`; `backend/tests/fixtures/expected-ocr/tesseract-5.5.3-evaluation-v2.json` |
| TD-017 | Wydajność alignmentu evaluatora | DP w `ocr-evaluator-v2` kopiuje krotkę `matches` w każdej komórce (`(*diagonal.matches, ...)`), czyli O(n·m·k) czasu i pamięci zamiast backpointerów | Skalowanie na długie ciągi znaków | ~0,5 dnia: backpointery i rekonstrukcja dopasowania po wypełnieniu tablicy | Brak dla cropów HUD (kilkanaście znaków); realne dopiero przy ewaluacji długich linii tekstu lub dużych zbiorów | Przepisać na backpointery, jeśli evaluator zacznie liczyć metryki dla pełnych klatek albo zbiorów rzędu tysięcy obserwacji. Trigger: mierzalny czas ewaluacji lub wejście dłuższe niż ~100 znaków | `backend/app/engines/definition/ocr_evaluation.py` |
| TD-018 | Testy sterujące workerem | `_wait_status` ma sztywny 5-sekundowy deadline i odpytuje realne wątki workera; przy obciążonej maszynie test przechodzi w izolacji, a pada w pełnym przebiegu | Deterministyczną synchronizację z workerem zamiast oczekiwania na czas | ~0,5 dnia, harness testowy | Fałszywy negatyw blokuje bramkę i uczy ignorowania czerwonych przebiegów — najgorszy możliwy nawyk przy tej wielkości suite'u | Zamienić deadline na oczekiwanie sterowane zdarzeniem albo skalować go względem zmierzonego czasu etapu. Zaobserwowane w re-review TK-007: `test_recovery_skip_then_resume_checkpoint_remains_valid_on_second_restart` padł w 53-minutowym przebiegu, przeszedł w dwóch 24-minutowych | `backend/tests/test_workflow_control_contract.py`; `backend/tests/test_durable_workflow.py` |
| TD-019 | Sprzątanie ACL w harnessie E2E | Scenariusz negatywny workspace nakłada realny deny `DELETE_CHILD` i zdejmuje go w `finally`; twarde zabicie całego procesu dokładnie między nałożeniem a `finally` zostawia chroniony ACL-em katalog `runtime-*` | Sprzątanie odporne na sygnały i twardy kill, niezależne od `finally` | ~0,5 dnia, launcher E2E i obsługa sygnałów | Ograniczone: chodzi o unikalny, jednorazowy leaf pod cache Playwrighta, nie o workspace produktu ani repozytorium; kolejny przebieg tworzy nowy `mkdtemp`, więc nie psuje ani następnych uruchomień, ani pracy aplikacji | Przenieść zdejmowanie ACL do handlera sygnałów launchera albo do sweepu startowego, który czyści osierocone leafy przed nowym przebiegiem. Zaobserwowane w niezależnym re-review TK-006-T2-FIX1; potwierdzone próbą z identycznymi ACE, która odmówiła zarówno unlinku probe, jak i rekurencyjnego cleanupu | `frontend/e2e/negative-flow.spec.ts:226`; `frontend/scripts/playwright.mjs:19`; `frontend/scripts/e2e-runtime-root.mjs:44` |
