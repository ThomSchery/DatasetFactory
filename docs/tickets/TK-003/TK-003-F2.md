# TK-003-F2 — domknięcie review granicy OCR

Status: TODO (kontynuacja od 2026-07-31)

## Cel

Domknąć końcowy review TK-003-F1 przed rozpoczęciem TK-004.

## Zakres

1. Wymusić zgodność `runtime.model.name == f"{language}.traineddata"` przed
   hashowaniem i uruchomieniem procesu. Mismatch ma zwracać
   `ocr_provenance_mismatch` bez wywołania runnera.
2. Zmienić alignment evaluatora na minimalny koszt edycji, a przy remisie
   maksymalny łączny IoU dla zgodnych znaków z zachowaniem kolejności.
   Dodać regresje dla `AA`/`AAA` i powtarzających się cyfr.
3. Pinować fixture przez manifest obejmujący ground truth i SHA-256 wszystkich
   referencjonowanych cropów; test ma wykrywać zmianę obrazu.
4. Ujednolicić `log.md` z aktualnym wynikiem: 59 GT, 60 obserwacji, 4 edycje,
   93,22% accuracy, 7/11 exact; TK-004 odblokować dopiero po zielonym review F2.

## Warunek zamknięcia

Targeted i pełne gate'y są zielone, a niezależny review potwierdza brak
actionable findings. TK-004 pozostaje poza zakresem tego ticketu.
