interface:
  display_name: "Immersive UI Factory"
  short_description: "Build and compare reusable visual systems"
  default_prompt: "Use $immersive-ui-factory to analyze this interface, produce versioned design iterations, and guide me toward a reusable final visual system."

policy:
  allow_implicit_invocation: true
